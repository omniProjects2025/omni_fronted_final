# UI Rules

- Use Bootstrap styles already present.
- Reuse existing CSS classes.
- Avoid inline styles.
- Maintain responsive behavior.
- Keep UI consistent with existing pages.
- Avoid redesigning unrelated screens.