{
  "/api/*": {
    "target": "http://localhost:3000",
    "secure": false,
    "changeOrigin": true,
    "logLevel": "debug",
    "ws": true,
    "headers": {
      "Accept": "application/json, multipart/form-data, */*",
      "Origin": "http://localhost:4200"
    },
    "onProxyReq": "function(proxyReq, req, res) { console.log('🔄 Proxying:', req.method, req.url); proxyReq.setHeader('Origin', 'http://localhost:4200'); }",
    "onProxyRes": "function(proxyRes, req, res) { console.log('✅ Proxy response:', proxyRes.statusCode, req.url); }",
    "onError": "function(err, req, res) { console.error('❌ Proxy error for', req.url, ':', err.message); res.writeHead(500, { 'Content-Type': 'text/plain' }); res.end('Proxy error: ' + err.message); }",
    "bypass": "function(req, res, proxyOptions) { if (req.headers['content-type'] && req.headers['content-type'].includes('multipart/form-data')) { console.log('📎 Multipart form detected for', req.url); } }"
  }
}
